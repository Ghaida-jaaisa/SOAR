@model Course?
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


<!DOCTYPE html>
<html>

<head>
    <title>NEW COURSE </title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            max-width: 600px;
            width: 100%;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            box-sizing: border-box;
        }

        h2 {
            text-align: center;
            color: #5a5a5a;
            margin-bottom: 20px;
        }

        /* Form Styles */
        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin: 10px 0 5px;
            font-weight: bold;
            color: #555;
        }

        input[type="text"], select {
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
            width: 100%;
            box-sizing: border-box;
        }

            input[type="text"]:focus, select:focus {
                border-color: #3498db;
                outline: none;
            }

        .buttons {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 20px;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

            button a {
                color: #fff;
                text-decoration: none;
            }

            button:first-child {
                background-color: #e74c3c;
            }

                button:first-child:hover {
                    background-color: #c0392b;
                }

            button[type="submit"] {
                background-color: #3498db;
                color: #fff;
            }

                button[type="submit"]:hover {
                    background-color: #2980b9;
                }
    </style>
</head>

<body>
    <div class="container">

        <h2>Add New Course</h2>
        <form asp-controller="Course" asp-action="Add" method="post">
            <div asp-validation-summary="All"></div>
            <label>Course Name</label>
            <input type="text" asp-for="@Model.CourseName" required />

            <label>Company Name</label>
            <input type="text" asp-for="@Model.ComapnyName" required />

            <label>Price</label>
            <input type="text" asp-for="@Model.Price" required/>

            <label>Duration</label>
            <input type="text" asp-for="@Model.Duration" required />

            <label asp-for="Location">Location</label>
            <select asp-for="@Model.Location" required>
                <option value="Jenin">Jenin</option>
                <option value="Nablus">Nablus</option>
                <option value="Ramallah">Ramallah</option>
                <option value="Hebron">Hebron</option>
                <option value="Jerusalem">Jerusalem</option>
                <option value="Bethlehem">Bethlehem</option>
                <option value="Gaza">Gaza</option>
                <option value="Rafah">Rafah</option>
            </select>


            <label asp-for="CategoryId">Category</label>
            <select asp-for="@Model.CategoryId" required>
                <option value="">select a Category</option>
                @foreach (Category c in ViewBag.Categories)
                {
                    <option value="@c.CategoryId">@c.CategoryName</option>
                }
            </select>

            <input type="hidden" asp-for="CourseId" />

            <div class="buttons">
                <button>
                    <a asp-controller="Course" asp-action="AdminCourses">Cancel</a>
                </button>
                <button type="submit">ADD</button>
            </div>
        </form>
    </div>
</body>
</html>

